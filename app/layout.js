import "./globals.css";

//todo: import the fonts
//todo: setup metadata on all pages for nice document tab titles

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
